compact_mode: true
hide_config: true
button_text:
  options: '{{ time }}'
exceptions:
  # Sarah doesn't need to see the debug or server screens
  - conditions:
      user: Sarah
    config:
      hide_tabs: 'debug, server'
  # Hide tabs on smaller devices
  - conditions:
      media_query: "(max-width: 600px)"
    config:
      hide_tabs: 'floorplan, environment'
      footer_mode: true
      indicator_top: true
      options_hide: true
